import React from 'react';
import Layout from '../components/layout/Layout';

const Settings = () => {
    return (
        <Layout>
            <div className="container">
                <h2 className="title">Settings</h2>
                <div className="card">
                    <p>Settings configuration functionality coming soon.</p>
                </div>
            </div>
        </Layout>
    );
};

export default Settings;
